import { ScrollAnimationDirectiveDirective } from './scroll-animation-directive.directive';

describe('ScrollAnimationDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollAnimationDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
