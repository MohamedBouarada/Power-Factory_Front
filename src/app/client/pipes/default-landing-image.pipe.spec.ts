import { DefaultLandingImagePipe } from './default-landing-image.pipe';

describe('DefaultLandingImagePipe', () => {
  it('create an instance', () => {
    const pipe = new DefaultLandingImagePipe();
    expect(pipe).toBeTruthy();
  });
});
